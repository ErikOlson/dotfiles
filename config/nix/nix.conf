# Enable nix-command and flakes globally.
# Without this, every nix invocation needs --extra-experimental-features.
# This file is symlinked to ~/.config/nix/nix.conf by setup.sh.
# Note: /etc/nix/nix.conf (system-level) is NOT modified — this is user-level only.
experimental-features = nix-command flakes
